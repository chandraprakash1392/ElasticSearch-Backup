# Elasticsearch-Backup
This code will take a backup of your running ElasticSearch 5.x into a remote server and shutdown the server once the backup is done.

# How to run the code:
git clone git@github.com:chandraprakash1392/Elasticsearch-Backup.git

cd Elasticsearch-Backup

Update the parameters.json file

python ./es_backup.py
